This example stack will deploy 2 example applications on:

* /${var.namespace}/foo/
* /${var.namespace}/bar/

Applications are pre-configured and user can pass in `app_version` which load load the provided version of the application image.